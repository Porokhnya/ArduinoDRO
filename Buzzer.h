#pragma once
//--------------------------------------------------------------------------------------------------------------------------------------
#include <Arduino.h>
#include "CONFIG.h"
//--------------------------------------------------------------------------------------------------------------------------------------
#ifdef USE_BUZZER
//--------------------------------------------------------------------------------------------------------------------------------------
class BuzzerClass
{
  public:
    BuzzerClass();
    void begin();
    void buzz(uint8_t times=1, uint16_t durOn = BUZZER_DURATION, uint16_t durOff = BUZZER_BETWEEN_IDLE);

   private:
   
    void buzzLevel(uint8_t level);
    static void buzzHandler();

    uint8_t currentLevel;
    uint8_t times;

    uint16_t durationOn, durationOff;
  
};
//--------------------------------------------------------------------------------------------------------------------------------------
extern BuzzerClass Buzzer;
//--------------------------------------------------------------------------------------------------------------------------------------
#endif // USE_BUZZER
